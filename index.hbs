{{!< default}}

<header class="subheader">
  <div class="container">
    <h2 class="subheader__page-title">{{@blog.title}} <small class="subheader__subtitle">{{@blog.description}}</small></h2>
  </div>
</header>

<main id="content" class="container">
  <div class="row">
    <div class="col-md-9">
      {{> "loop"}}
    </div>
    <div class="col-md-3">
      {{> "tags"}}
    </div>
  </div>
</main>
